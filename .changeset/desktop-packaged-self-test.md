---
"cycling-coach": patch
---

User-facing: Added `enduragent self-test` to verify the installed desktop app's metric bundle.

Add an authenticated packaged-app self-test with a Vitest-free runner, dual-location SHA-256 resource verification, and stable machine-readable exits.
