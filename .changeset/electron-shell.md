---
"cycling-coach": patch
---

User-facing: Added the macOS desktop shell and direct activity import and training-data sync commands.

Add the Electron launch surface, supervised local service connection, protocol-2 operational RPC, and renderer security boundary.
