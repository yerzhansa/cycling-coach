# Example

## Sub-features

- `scenario-id`: user-visible capability — executor `playwright`; fixture `ready`; proof is the resulting accessible state.

## How to get to it (user POV)

Open the named surface through every supported user entry point.

## Driving it with verify-example

```bash
pnpm verify:example -- verify --feature example
```

Require the action, resulting visible state, and a second read-only view of any persisted change.

## Gotchas

- Record platform, authentication, entitlement, and external-state prerequisites here.
