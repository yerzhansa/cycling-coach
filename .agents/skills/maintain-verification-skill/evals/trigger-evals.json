[
  {
    "query": "Use $maintain-verification-skill to audit verify-enduragent's Chat and past chats feature map against current source and live behavior. If every route and executor still aligns, make no changes and return clean.",
    "should_trigger": true
  },
  {
    "query": "Use $maintain-verification-skill on verify-enduragent: the Training page still cites a renamed Vitest file and its runner command is stale. Correct only the owned verification map or runner, then recheck the affected feature.",
    "should_trigger": true
  },
  {
    "query": "Use $maintain-verification-skill to audit Settings connections after local connection deletion stopped working even though the map and runner match current source. Retain the evidence and report the product regression without editing product source or product tests.",
    "should_trigger": true
  },
  {
    "query": "Use $create-verification-skill to create the first verification skill for a separate desktop app's Setup flow and reuse its existing CDP harness.",
    "should_trigger": false
  },
  {
    "query": "Run ordinary desktop UI QA on Enduragent Setup: complete the first-run gate, confirm Main navigation appears, and report the isolated evidence.",
    "should_trigger": false
  },
  {
    "query": "Audit and repair the stale shell-lifecycle feature page in verify-enduragent, keeping the existing Playwright and Vitest ownership boundaries intact.",
    "should_trigger": false
  }
]
