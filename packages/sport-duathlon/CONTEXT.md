# Sport-Duathlon (placeholder)

Not yet implemented. Tracked in issue #47. When implemented, this context will own brick workouts, transitions, dual periodization. Coordinator pattern per ADR-0002 — composes `@enduragent/sport-cycling` and `@enduragent/sport-running` rather than redefining their vocabulary. Ships as `duathlon-coach` binary.

## Status: alpha — empty stub

Private workspace package — not published to npm. Becomes a published `@enduragent/sport-duathlon` (SemVer) when a real Sport implementation lands and an external consumer needs it. See ADR-0009.
