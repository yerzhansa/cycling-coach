#!/usr/bin/env node
const PKG_VERSION = "2026.4.30"; // Bumped at publish via changesets/CalVer.
console.log(`Duathlon Coach v${PKG_VERSION} (alpha — not yet implemented)`);
console.log("Status: scaffolding only. Real implementation imminent.");
console.log("Track: https://github.com/yerzhansa/cycling-coach/issues/47");
process.exit(0);
